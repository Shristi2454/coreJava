package overloading;

public class OverloaddingDocs {

	/*
	 * -> overloading and overriding to achieve polymorphism
	 * ->same name method in the same class is overloading(same name, same return type but different list of arguments(parameters)) 
	 * -> same number of arguments but different datatype
	 * 
	 */
}
